package Stringss;

public class ReverseWordsinaString {
    public static void main(String[] args) {
        // class Solution {
        //     public String reverseWords(String s) {
        //         // Step 1: Trim the string to remove leading and trailing spaces
        //         s = s.trim();
                
        //         // Step 2: Split the string into words based on spaces
        //         String[] words = s.split("\\s+"); // This handles multiple spaces
                
        //         // Step 3: Reverse the array of words
        //         StringBuilder sb = new StringBuilder();
        //         for (int i = words.length - 1; i >= 0; i--) {
        //             sb.append(words[i]);
        //             if (i > 0) {
        //                 sb.append(" "); // Append a space between words
        //             }
        //         }
        
        //         // Step 4: Return the result as a string
        //         return sb.toString();
        //     }
        // }

        // class Solutions {
        //     public String reverseWords(String s) {
        //         // Step 1: Trim the string to remove leading and trailing spaces
        //         s = s.trim();
                
        //         // Step 2: Split the string into words based on spaces
        //         String[] words = s.split("\\s+"); // This handles multiple spaces
                
        //         // Step 3: Reverse the array of words
        //         StringBuilder sb = new StringBuilder();
        //         for (int i = words.length - 1; i >= 0; i--) {
        //             sb.append(words[i]);
        //             if (i > 0) {
        //                 sb.append(" "); // Append a space between words
        //             }
        //         }
        
        //         // Step 4: Return the result as a string
        //         return sb.toString();
        //     }
        // }
        
    }
}